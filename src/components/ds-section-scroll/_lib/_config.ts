const config = {
  // 
  bodyBlockClass: "ds-section-scroll-overflow-hidden",
  scrollContainer: ".ds-section-scroll-container",
  scrollItem: ".ds-section-scroll-container",
  // 
  sections: new Array,
  state: 0,
  delta: 0,
  currentID: 0,
  // 
  variant: 1,
  zIndex: 1000,
  duration: 500,
  logs: false,
};

export default config;
